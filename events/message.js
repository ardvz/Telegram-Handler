module.exports = (bot, msg) => {
    // Ingin mengirim pesan ke grub
    // const id = Number('id grub');
    // bot.sendMessage(id, 'Pesan botnya');

    // Tambahkan setInterval jika ingin mengirim pesan secara berulang
};
